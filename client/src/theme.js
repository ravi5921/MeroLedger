import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#03045E",
    },
    secondary: {
      main: "#F1FAEE",
    },
    warning: {
      main: "#E63946",
    },
  },
});

export default theme;
